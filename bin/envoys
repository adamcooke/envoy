#! /usr/bin/env ruby
# -*- mode: ruby -*-

begin
  require 'envoy/server/command'
rescue LoadError
  path = File.expand_path '../../lib', __FILE__
  $:.unshift path if File.directory?(path)
  require 'envoy/server/command'
end

